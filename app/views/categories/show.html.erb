<h1><%= @category.name %></h1>
<%= form_for(Game.new)  do |f| %>
<%= f.hidden_field :category_id, value: @category.id%>
<%= f.hidden_field :trivia_game_id, value: 1%>
<%= f.hidden_field :user_id, value: current_user.id %>
<%= f.submit 'Start Game'%>
<% end %>
