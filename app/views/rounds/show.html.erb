 <% if flash[:msg] %>
 <% flash.each do |type, msg| %>
  <div>
    <h1><%= msg %></h1>
  </div>
  <% end %>
<% end %>
 <% if current_user.questions.count < 5 %>
<%= link_to "Next Round", new_game_round_path(@game)%>
<% else %>
<%= link_to 'End Game', game_delete_rounds_path %>
<% end %>